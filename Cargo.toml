[workspace]
members = [
    "programs/*",
    "tests",
]
resolver = "2"
rust-version = "1.81.0" # Required by Anchor

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
